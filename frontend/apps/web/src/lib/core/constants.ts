export const PAGINATION = {
  /** Default number of items to load in paginated endpoints */
  PAGE_SIZE: 100
} as const;

export const DEFAULT_LANDING_PAGE = "/spaces/personal/chat";
export const DASHBOARD_URL = "/dashboard";

export const ATTACHMENTS = {
  /** Maximum nuber of files to upload at a time */
  MAX_CONCURRENT_UPLOADS: 5
};
